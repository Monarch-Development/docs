---
title: Installation
icon: Download
---

## Steps

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
  <Step>
  Download the resource (main or release)
  </Step>
  <Step>
  Extract the .zip file
  </Step>
  <Step>
  Make sure the resource name is `mnr_elevators` to make it work properly
  </Step>
  <Step>
  Make sure you install all the listed below dependencies and run them following the order
  </Step>
</Steps>

## Dependencies and Start Order

```mdx title="server.cfg"
ensure ox_lib
ensure (FRAMEWORK)
ensure (INVENTORY)
ensure mnr_elevators
```

<Callout title="WARNING" type="warn">
  The `(FRAMEWORK)` and `(INVENTORY)` parts are just indicative, replace them with the names of the resources you use.
</Callout>